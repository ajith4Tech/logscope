# Copy to s3-secret.yaml, fill in real values, kubectl apply -f — never commit the filled version.
apiVersion: v1
kind: Secret
metadata:
  name: logscope-s3-creds
  namespace: logging
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: ""
  AWS_SECRET_ACCESS_KEY: ""
